<Window x:Class="Cafe_Kiosk.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cafe_Kiosk.Views"
        xmlns:vm="clr-namespace:Cafe_Kiosk.ViewModels"
        mc:Ignorable="d"
        Height="1280" Width="720"
        Background="LightBlue">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MenuViewModel}">
            <local:MenuView/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!--Logo-->
        <TextBlock Grid.Row="0"
                   Text="Cafe Kiosk Program"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   FontWeight="Bold"
                   FontSize="20"/>

        <!--Menu View-->
        <ContentControl Grid.Row="1"
                        Margin="10"
                        Content="{Binding CurrentViewModel}"/>
        
        <Grid Grid.Row="2"
              Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/> 
            </Grid.ColumnDefinitions>

            <!--Cart View-->
            <local:CartView Grid.Column="0"
                            Margin="10"
                            DataContext="{Binding _cartViewModel}"/>

            <!--Payment View-->
            <local:PaymentView Grid.Column="1"
                               Margin="10"
                               DataContext="{Binding _paymentViewModel}"/>
        </Grid>
    </Grid>
</Window>
