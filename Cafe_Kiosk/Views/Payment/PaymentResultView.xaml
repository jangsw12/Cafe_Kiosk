<UserControl x:Class="Cafe_Kiosk.Views.Payment.PaymentResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cafe_Kiosk.Views.Payment" 
             xmlns:fa6="http://schemas.fontawesome.com/icons/svg"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="720">
    <Grid Background="Transparent"
          Margin="20">
        <!--Cart Items List-->
        <StackPanel Margin="0 40"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Orientation="Vertical">

            <!--Icon-->
            <fa6:SvgAwesome Icon="{Binding ResultIcon}"
                            PrimaryColor="{Binding ResultColor}"
                            Width="80" Height="80"
                            Margin="0 0 0 20"/>

            <!--Result Message-->
            <TextBlock Text="{Binding ResultMessage}"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{Binding ResultColor}"
                       TextAlignment="Center"
                       Margin="0 0 0 20"/>

            <!--Add Info-->
            <TextBlock Text="잠시 후 영수증 선택 화면으로 이동합니다."
                       FontSize="16"
                       Foreground="Gray"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
